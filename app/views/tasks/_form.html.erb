<%= form_with(model: tasks) do |f| %>

  <% if tasks.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tasks.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
        <% tasks.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <%= f.label :status, 'ステータス' %>
  <%= f.text_field :status %>
  
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit '作成' %>
<% end %>
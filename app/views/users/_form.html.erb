<%= form_with(model: users) do |f| %>  
  
  <% if users.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(users.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
        <% users.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<% end %>